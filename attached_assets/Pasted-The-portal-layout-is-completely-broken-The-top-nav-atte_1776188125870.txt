The portal layout is completely broken. 
The top nav attempt failed — nav items 
are hanging down the left as a vertical 
list and the main content is blank.

REVERT the layout back to the original 
LEFT SIDEBAR structure that was working 
before. Then apply visual styling changes 
ONLY — do not touch the layout structure.

The original working layout was:
- Fixed left sidebar (240px wide)
- Main content area fills the rest
- Logo/brand at top of sidebar
- Nav items listed vertically in sidebar
- Logout at bottom of sidebar

Keep that exact structure. 
DO NOT move the sidebar to a top nav.
DO NOT change any layout or positioning.
ONLY change colours, fonts and visual style.

===============================================================
STEP 1 — RESTORE ORIGINAL SIDEBAR LAYOUT
===============================================================

Find the portal.html file and restore 
the sidebar to its original left-side 
fixed position layout.

The sidebar must be:
position: fixed (or the equivalent)
left: 0
top: 0
width: 240px
height: 100vh
overflow-y: auto

The main content must be:
margin-left: 240px
min-height: 100vh

If the top nav HTML was added during 
the last change — remove it completely 
and restore the sidebar HTML.

===============================================================
STEP 2 — VISUAL RESKIN ONLY (SIDEBAR STAYS LEFT)
===============================================================

Once the layout is restored apply these 
visual changes only:

SIDEBAR BACKGROUND:
Change from cream/off-white to dark:
background: #1a0f00;

SIDEBAR NAV ITEMS:
color: #c8a882; (warm cream)
font-family: 'DM Sans', sans-serif;
font-size: 13px;

SIDEBAR ACTIVE ITEM:
background: rgba(200, 134, 10, 0.15);
color: #c8860a;
border-left: 3px solid #c8860a;
border-radius: 0 8px 8px 0;

SIDEBAR HOVER:
background: rgba(255,255,255,0.05);
color: #f5ede0;

SIDEBAR LOGO/BRAND AREA:
background: #0f0800; (slightly darker)
border-bottom: 1px solid rgba(200,134,10,0.2);
padding: 20px 16px;

Brand name: color: #c8860a; font-family: Playfair Display;
Subtitle: color: #6b5040; font-size: 10px; letter-spacing: 2px;

SIDEBAR SECTION LABELS (OVERVIEW, BUSINESS, SYSTEM):
color: #4a3828;
font-size: 9px;
letter-spacing: 2px;
text-transform: uppercase;
padding: 16px 16px 6px;

SIDEBAR LOGOUT BUTTON:
color: #6b5040;
border-top: 1px solid rgba(200,134,10,0.15);
padding: 16px;

MAIN CONTENT BACKGROUND:
background: #faf6f1; (warm cream — not white)

MAIN TOP BAR:
background: #ffffff;
border-bottom: 1px solid #ede5d8;
padding: 16px 28px;
display: flex;
align-items: center;
justify-content: space-between;

PAGE TITLE:
font-family: 'Playfair Display', serif;
font-size: 24px;
color: #1a0f00;

STAT CARDS:
background: #ffffff;
border-radius: 12px;
border: 1px solid #ede5d8;
border-left: 4px solid #c8860a;
box-shadow: 0 2px 8px rgba(26,15,0,0.05);

Stat value: font-family: Playfair Display; 
font-size: 30px; color: #1a0f00;
Stat label: font-size: 10px; 
letter-spacing: 1.5px; color: #a08060;

Alternate left border colours per card:
Card 1 (Revenue): border-left-color: #c8860a
Card 2 (Appointments): border-left-color: #7c9e87
Card 3 (Clients): border-left-color: #9b7ea6
Card 4 (Avg Spend): border-left-color: #c8860a

SECTION CARDS (Recent Bookings etc):
background: #ffffff;
border: 1px solid #ede5d8;
border-radius: 14px;
box-shadow: 0 2px 12px rgba(26,15,0,0.04);

Card header:
background: #fdfaf7;
border-bottom: 1px solid #f0e8de;
font-family: Playfair Display;
font-size: 15px;
color: #1a0f00;

NEW BOOKING BUTTON:
background: #1a0f00;
color: #f5ede0;
border-radius: 8px;
font-size: 13px;
font-weight: 600;

EXPORT BUTTON:
border: 1px solid #c8860a;
color: #c8860a;
background: transparent;
border-radius: 8px;

SEARCH BAR:
background: #faf6f1;
border: 1px solid #ede5d8;
border-radius: 8px;

===============================================================
STEP 3 — LOGIN SCREEN DARK THEME
===============================================================

The login page (the 5-click secret screen) 
should have a dark luxe look:

Login page background:
background: #0f0800;

Add subtle radial gradient:
background: radial-gradient(ellipse at center, 
  #1a0f00 0%, #0a0500 100%);

Login card:
background: rgba(255,255,255,0.04);
border: 1px solid rgba(200,134,10,0.25);
border-radius: 20px;
backdrop-filter: blur(20px);
padding: 40px;
max-width: 380px;
box-shadow: 0 20px 60px rgba(0,0,0,0.4);

Brand wordmark on login:
color: #c8860a;
font-family: Playfair Display;
font-size: 22px;
font-weight: 700;
letter-spacing: 3px;
text-transform: uppercase;

Subtitle:
color: #6b5040;
font-size: 10px;
letter-spacing: 4px;

Password input:
background: rgba(255,255,255,0.06);
border: 1px solid rgba(200,134,10,0.3);
border-radius: 10px;
color: #f5ede0;
padding: 13px 16px;
font-size: 14px;

Password input focus:
border-color: #c8860a;
outline: none;
box-shadow: 0 0 0 3px rgba(200,134,10,0.15);

Login button:
background: #c8860a;
color: #1a0f00;
border: none;
border-radius: 10px;
padding: 13px;
font-weight: 700;
font-size: 14px;
width: 100%;
letter-spacing: 0.5px;

Login button hover:
background: #e09a0c;

"Click 5 times to reveal login" hint text:
color: #4a3828;
font-size: 12px;

===============================================================
STEP 4 — TYPOGRAPHY
===============================================================

Add to <head> of portal.html if not there:
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600;700&family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;0,9..40,600&display=swap" rel="stylesheet">

Apply:
Headings, page titles, card titles, 
stat values, brand name → Playfair Display

Everything else → DM Sans

===============================================================
STEP 5 — STATUS PILL STYLES
===============================================================

.status-confirmed {
  background: #e6f4ec;
  color: #1e5c33;
  border: 1px solid #b8dcc4;
  border-radius: 20px;
  padding: 3px 12px;
  font-size: 11px;
  font-weight: 600;
  letter-spacing: 0.3px;
}

.status-pending {
  background: #fef4e2;
  color: #7a4800;
  border: 1px solid #f5d49a;
  border-radius: 20px;
  padding: 3px 12px;
  font-size: 11px;
  font-weight: 600;
}

.status-cancelled {
  background: #fdeaea;
  color: #8b2020;
  border: 1px solid #f5b8b8;
  border-radius: 20px;
  padding: 3px 12px;
  font-size: 11px;
  font-weight: 600;
}

===============================================================
CRITICAL RULES
===============================================================

- Sidebar stays on the LEFT — do not move it
- All navigation functionality must still work
- Every page/section uses the new dark sidebar
- Main content area stays cream #faf6f1
- Do NOT break any JavaScript or data logic
- Do NOT change any API calls or form handling
- Test by clicking through Dashboard, Schedule, 
  Clients, Treatments, Finance pages and confirm 
  they all load correctly
- Login screen must still work with the 5-click 
  mechanic and correct password

Republish after all changes then 
reload portal.html to verify.